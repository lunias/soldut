# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this project is

Soldut is a 2D side-scrolling multiplayer mech shooter in C, in the lineage of Soldat. The current build is **M6 — IK & pose sync + jetpack propulsion FX + capped internal render target + damage numbers + editor-runtime parity & atmospherics (P01–P04 + P09 shipped, P09 2026-05-15)**. M6 P09 closes the editor↔runtime parity gap and adds a per-map atmospherics layer. **Tile flag visual differentiation**: every `TILE_F_*` bit (SOLID/ICE/DEADLY/ONE_WAY/BACKGROUND) now paints visually distinct in-game via a per-flag material lookup in new module `src/atmosphere.{c,h}` — ICE tiles get a sin-driven glint stripe, DEADLY tiles a 45° amber-red hatch, ONE_WAY tiles a '^' chevron, BACKGROUND tiles fold to 60% alpha. Atlas-path tints multiply into `DrawTexturePro`'s colDiffuse; the no-atlas fallback paints a theme-tinted 2-tone checkerboard. **Polygon visual hygiene**: `POLY_KIND_DEADLY` color fixed `{80,200,80}` → `{200,60,60}` (matching the editor's red and thumbnail's red — was clashing green); per-kind fill colors now sourced from `g_themes[]` so the same NEON map paints magenta DEADLY + cyan ICE polys. ICE polys get a glint stripe along the longest edge; DEADLY polys get a hatch overlay. **Dead-bit physics fix**: `TILE_F_ONE_WAY` + `TILE_F_BACKGROUND` collision finally wired through `src/physics.c::collide_map_one_pass` (was only honored on `POLY_KIND_*` analogs at lines 599-600/717-721; the editor's checkbox UI lied about both tile flags). New `level_ray_hits_kinematic` in `src/level.c` treats ONE_WAY as non-blocking when the integrate sweep is moving UPWARD so jet-through-platform works (paired with a velocity-sign gate in collide_one_pass). **Ambient zones get visuals**: each of the 4 `AmbiKind` values (WIND/ZERO_G/ACID/FOG) now produces a rect-scoped on-screen effect via `atmosphere_draw_ambient_zones` + 3 new `FxKind`s (`FX_AMBIENT_WIND_STREAK` / `_ZEROG_MOTE` / `_ACID_BUBBLE`). `AMBI_FOG` (previously INERT, zero refs in src/) earns its game effect: rect-bounding disc collected into the new `fog_zones[16]` shader uniform array (peer of M6 P02's `jet_hot_zones[16]`) and rendered as a per-fragment haze in the halftone shader's new fog-volume pass. **`LvlMeta.reserved[9]` promoted** to 9 named atmosphere fields (`theme_id`, `sky_top_rgb565`, `sky_bot_rgb565`, `fog_density_q`, `fog_color_rgb565`, `vignette_q`, `sun_angle_q`, `weather_kind`, `weather_density_q`); fits in the existing 18-byte reserved slot so `LvlMeta` stays 32 B + no `LVL_VERSION` bump + pre-P09 .lvls read zeros = use defaults. **7 theme palettes** (CONCRETE / BUNKER / ICE_SHEET / NEON / RUST / OVERGROWN / INDUSTRIAL) in `g_themes[]`. **Sky gradient**: paints into the internal RT via `DrawRectangleGradientV` from `atmosphere_draw_sky`, replacing the hardcoded `{12,14,18}` clear. **Shader extension**: `assets/shaders/halftone_post.fs.glsl` adds 5 atmospherics uniforms (`fog_density`, `fog_color`, `fog_zones[16]`, `fog_zone_count`, `vignette_strength`, `atmos_time`) — all behind a `<= 0.001` short-circuit so a zero-init map costs zero shader work; halftone + shimmer passes untouched. **4 weather modes** (SNOW/RAIN/DUST/EMBERS) via 4 new `FX_WEATHER_*` FxKinds spawned in screen-space (pos.x/pos.y normalized 0..1) by `atmosphere_tick`; rendered at window resolution AFTER the internal-RT upscale via `atmosphere_draw_weather` so flakes/raindrops land at sharp window pixels regardless of the M6 P03 cap. `FxPool` capacity bumped `8500 → 10500` (+112 KB) to cover combat + weather + ambient stacking. **Editor parity**: `tools/editor/tool.h` extended with `ambi_strength`/`ambi_dir_deg`/`deco_scale`/`deco_rot_deg`/`deco_flipped_x`/`deco_additive`; new `ui_draw_deco_palette` panel (layer/scale/rot/flip/additive sliders); AMBI palette grows per-kind strength + direction sliders; META modal grows by ~300 px to fit an Atmospherics panel (theme cycle, fog density/vignette sliders, weather kind cycle + density, music + ambient_loop text fields). Editor's tool.c writeback now uses the new ToolCtx fields instead of hardcoded `16384/32767/0`. **8 maps recooked** by `tools/cook_maps/cook_maps.c` with per-map themes per §10.7 of the spec (foundry=INDUSTRIAL, slipstream=ICE_SHEET+SNOW@0.30, reactor=RUST+EMBERS@0.40, concourse=CONCRETE, catwalk=NEON, aurora=NEON+DUST@0.20, crossfire=CONCRETE, citadel=BUNKER+DUST@0.30). New helper `set_meta_atmos()` + `rgb565()` + `q0_16()` in cook_maps. **2 new SFX entries** (`SFX_ENV_ZEROG_HUM`, `SFX_ENV_ACID_BUBBLE`) — 52 manifest entries total; per-zone audio fired by `atmosphere_tick` when the local mech enters a zone of that kind (edge-triggered, no continuous loop). **Asset sourcing**: new `tools/sprite_inventory/source_map.sh` (peer of audio_inventory) documents the Kenney + OpenGameArt CC0 packs feeding `tiles_default.png` / `decorations.png` / `caustic_acid.png` / `fog_noise.png`; runtime falls back to procedural visuals when raw packs aren't downloaded. **New shotmode directives** `paint_tile <tx> <ty> <flags_hex>`, `paint_rect <tx0> <ty0> <tx1> <ty1> <flags_hex>`, `paint_ambi <kind> <wx0> <wy0> <wx1> <wy1> [strength dx dy]` apply on top of the loaded map before tick 0, used by the per-flag visual regression tests. New tests: `tests/shots/m6_p09_tile_flags.shot`, `m6_p09_ambient_zones.shot`, `m6_p09_atmosphere_themes.shot`, `m6_p09_tile_oneway_collide.shot`, `make test-atmosphere-parity` paired-process (`tests/shots/net/run_atmosphere_parity.sh` — 10/10 assertions, host + 2 clients all log identical `atmosphere: theme=2 sky=... fog=... weather=1@0.30` lines on Slipstream). Protocol id stays `S0LK` (no wire changes — `.lvl` distribution via P08 carries the new atmosphere bytes for free). `test-level-io` gains 9 explicit per-field round-trip assertions on the promoted atmosphere block; `test-snapshot` / `test-pickups` / `test-ctf` / `test-spawn` / `test-prefs` / `test-map-share` / `test-grapple-ceiling` / `tests/shots/net/run.sh 2p_basic` all still pass. See `documents/m6/09-editor-runtime-parity-and-atmospherics.md`.

Previously: M6 P04 ships flying damage-number glyphs: a new `FX_DAMAGE_NUMBER` kind in the existing `FxPool` (cap bumped 8000 → 8500, +8 B per particle for `angle`/`ang_vel` tumble state) that spawns from every successful damage event via two callers — server-side `mech.c::mech_apply_damage` (visible in single-process / offline) and client-side `net.c::client_handle_hit_event` (the loopback path that's visible on the host UI per wan-fixes-16 + every remote peer). Same two-caller pattern blood/sparks already use; no new wire bytes (`NET_MSG_HIT_EVENT`'s 12-byte payload already carries the damage byte). New 5-tier color/size table in `src/particle.c::g_dmg_tier` keyed off the damage byte (LIGHT pale-yellow 20 px → CRIT bright-red 44 px @ 1× zoom); thresholds (10/30/60/100) intentionally mirror the M5 P12 decal cliffs so a 75-dmg hit paints a SCORCH decal AND shows an orange-red "75" glyph. Per-tick physics (`fx_update` new `FX_DAMAGE_NUMBER` branch): gravity with a 0.30 s hover-ramp so the number pops + hangs before falling, ±1 rad/s gentle tumble, 0.99 air drag, tile-collide BOUNCE up to 2× (each contact plays `SFX_DAMAGE_TINK` at 0.30 volume + damps spin to 60%), then rest pinned to the floor until the 0.5 s alpha-fade tail expires. Render pass (`fx_draw_damage_numbers`) runs OUTSIDE the internal-RT world pass — invoked from `renderer_draw_frame` after the upscale blit and before `hud_draw` so glyphs land at sharp window pixels (drawing inside the RT would bilinear-blur the text on upscale, same reason M6 P03 routes the HUD around the cap). Font size scales by `camera.zoom * blit_scale` so a CRIT reads at ~62 px on 1080p and ~124 px on a 4K-windowed run with the 1080-cap internal RT. 4-pass cardinal-offset outline (cheaper than a shader, visually equivalent at small sizes). New shotmode directive `damage <mech_id> <part> <amount>` exercises every tier from a single-process script. New SFX `SFX_DAMAGE_TINK` sourced from `kenney_impact-sounds/impactMetal_light_000.ogg` (capped 180 ms with 50 ms fade-out, 3 round-robin aliases); 50 SFX manifest entries total now (47 base + 2 M6 P02 ignition + 1 M6 P04 tink). Protocol id stays `S0LK` (no wire changes). New tests `test-damage-numbers` (paired host/client sync on aurora: host fires 3 Pulse Rifle shots, both sides assert 3 spawn lines with matching dmg/tier columns, 5 assertions) + `tests/shots/m6_damage_number_tiers.shot` (single-process visual review of all 5 tiers on slipstream, contact sheet composites 4 frames spanning pre-damage → spawn → midflight → settled). See `documents/m6/08-damage-numbers.md`. M6 P04 ships flying damage-number glyphs: a new `FX_DAMAGE_NUMBER` kind in the existing `FxPool` (cap bumped 8000 → 8500, +8 B per particle for `angle`/`ang_vel` tumble state) that spawns from every successful damage event via two callers — server-side `mech.c::mech_apply_damage` (visible in single-process / offline) and client-side `net.c::client_handle_hit_event` (the loopback path that's visible on the host UI per wan-fixes-16 + every remote peer). Same two-caller pattern blood/sparks already use; no new wire bytes (`NET_MSG_HIT_EVENT`'s 12-byte payload already carries the damage byte). New 5-tier color/size table in `src/particle.c::g_dmg_tier` keyed off the damage byte (LIGHT pale-yellow 20 px → CRIT bright-red 44 px @ 1× zoom); thresholds (10/30/60/100) intentionally mirror the M5 P12 decal cliffs so a 75-dmg hit paints a SCORCH decal AND shows an orange-red "75" glyph. Per-tick physics (`fx_update` new `FX_DAMAGE_NUMBER` branch): gravity with a 0.30 s hover-ramp so the number pops + hangs before falling, ±1 rad/s gentle tumble, 0.99 air drag, tile-collide BOUNCE up to 2× (each contact plays `SFX_DAMAGE_TINK` at 0.30 volume + damps spin to 60%), then rest pinned to the floor until the 0.5 s alpha-fade tail expires. Render pass (`fx_draw_damage_numbers`) runs OUTSIDE the internal-RT world pass — invoked from `renderer_draw_frame` after the upscale blit and before `hud_draw` so glyphs land at sharp window pixels (drawing inside the RT would bilinear-blur the text on upscale, same reason M6 P03 routes the HUD around the cap). Font size scales by `camera.zoom * blit_scale` so a CRIT reads at ~62 px on 1080p and ~124 px on a 4K-windowed run with the 1080-cap internal RT. 4-pass cardinal-offset outline (cheaper than a shader, visually equivalent at small sizes). New shotmode directive `damage <mech_id> <part> <amount>` exercises every tier from a single-process script. New SFX `SFX_DAMAGE_TINK` sourced from `kenney_impact-sounds/impactMetal_light_000.ogg` (capped 180 ms with 50 ms fade-out, 3 round-robin aliases); 50 SFX manifest entries total now (47 base + 2 M6 P02 ignition + 1 M6 P04 tink). Protocol id stays `S0LK` (no wire changes). New tests `test-damage-numbers` (paired host/client sync on aurora: host fires 3 Pulse Rifle shots, both sides assert 3 spawn lines with matching dmg/tier columns, 5 assertions) + `tests/shots/m6_damage_number_tiers.shot` (single-process visual review of all 5 tiers on slipstream, contact sheet composites 4 frames spanning pre-damage → spawn → midflight → settled). See `documents/m6/08-damage-numbers.md`. M5 P01–P19 shipped + off-roadmap **wan-fixes 1–16** WAN polish pass + **M6 P01** (IK + pose sync) + **M6 P02** (jetpack propulsion FX) + **M6 P03** (capped internal render target — finally builds the v1 `documents/10-performance-budget.md:208` commitment: world + halftone+shimmer post-process pass now rasterise into a capped-height internal RT (default 1080 lines, width derived from window aspect via `platform_begin_frame`'s `internal_w = round(render_w * cap / render_h)`) instead of the backbuffer; result bilinear-upscales to the window with aspect-preserving letterbox (same `MIN(sw/iw, sh/ih)` pattern as raylib's `core_window_letterbox` example). HUD + overlay callbacks + audio overlays continue to draw at window resolution unshaded — sharp text/glyphs at the user's monitor pixel count regardless of the cap. New `g_internal_target` peer of `g_post_target` in `src/render.c`; `ensure_internal_targets(iw, ih)` manages both at the capped size with point filter (the upscale blit flips `g_post_target`/`g_internal_target` to bilinear right before draw). `renderer_draw_frame` signature is now `(internal_w, internal_h, window_w, window_h, ...)`. Cursor → world conversion goes window → internal (via new `Renderer.blit_scale/dx/dy` fields written each frame from the same letterbox numbers) → world (`renderer_screen_to_world` runs the inverse); aim under the cursor stays visually correct. `hud_draw` receives a synthesised window-space camera (`zoom *= scale`, `offset = camera.offset * scale + (dx, dy)`) so off-screen indicators like the CTF flag compass land at the right window position. **MSAA dropped** from the backbuffer (`platform.c` no longer sets `FLAG_MSAA_4X_HINT`); the halftone screen masks sub-pixel aliasing the world had paid 4× cost for, HUD is rects + bilinear-sampled glyphs neither of which benefit from MSAA, ~57 MB VRAM + ~1 ms / frame back. New `internal_res_h` setting in `ServerConfig` (`soldut.cfg`, default 1080, alias `render_height=`) + `UserPrefs` (`soldut-prefs.cfg`, persisted across launches; prefs > cfg > built-in). New CLI flags `--internal-h N`, `--perf-overlay`, `--window WxH`, `--fullscreen` for interactive A/B verification. New shotmode directives `internal_h N` and `perf_overlay on|off`; opt-in `g_shot_perf_overlay` global (peer of `g_shot_mode` in `log.{c,h}`) drives an on-screen FPS readout + a per-second `SHOT_LOG perf: fps=N internal=WxH window=WxH scale=S` line. Existing shot tests stay byte-identical because shotmode defaults `internal_h = 0` (no cap) → internal RT is 1:1 with the window → pipeline reduces to the pre-P03 shape. The hot-zone screen coords from `mech_jet_fx_collect_hot_zones` are already in the camera's internal coordinate system, so no scaling is needed for the shader's shimmer pass. New bench scripts `tests/shots/perf_bench{,_capped}.shot` + `perf_stress{,_capped}.shot` exercise no-cap vs cap-1080 at 3440×1440. Measured (this dev's WSL2/WSLg machine, 3440×1440): stress bench 22.7 s wall (no cap) vs 20.9 s (cap) = 8 % wall-clock win — modest because WSLg compositor frame-paces both, not GPU-bound here; on native Windows/Linux where GPU pixel cost is the dominant frame cost, the cap restores the documented 1080-line budget. Sync/regression status post-fix: all asserting unit tests + paired-process tests pass (`test-level-io`, `test-pickups`, `test-ctf`, `test-snapshot`, `test-spawn`, `test-prefs`, `test-map-*`, `test-grapple-ceiling`, `test-frag-grenade`, `tests/net/run_3p.sh`, paired `2p_basic`/`2p_legs`/`2p_dismember` shots, `test-meet-named`, `test-meet-custom`). Pre-existing `run_ctf_walk_own_flag.sh` flake remains. Single-process gravity verified via `m1_big_fall.shot` (mech falls + settles). See `documents/m6/03-perf-4k-enhancements.md`). M6 P02 ships a new `src/mech_jet_fx.{c,h}` module (per-jetpack tuning table + per-chassis nozzle table + per-tick exhaust spawn + plume-quad render path + screen-space heat-shimmer hot zones) wired into `simulate.c` (FX step after `pose_compute`) and `render.c` (plume between decals and mech loop; shimmer folded into the existing halftone post-process pass with a 16-slot `vec4 jet_hot_zones[]` uniform array + `jet_time` float, shimmer-skip flag set in shot mode for deterministic screenshots). Two new `FxKind`s — `FX_JET_EXHAUST` (additive, hot→cool color lerp + buoyancy) and `FX_GROUND_DUST` (alpha, gravity + drag) — share the existing `FxPool` (capacity bumped 3000 → 8000 to cover Burst-jet worst case at 16 mechs × 2 nozzles × 8 particles/tick × 60 Hz × 0.5 s avg life). New per-mech `jet_state_bits` (MECH_JET_ACTIVE / IGNITION_TICK / BOOSTING) + `jet_prev_grounded` are owner-side written by `mech_step_drive` and remote-side written by `snapshot_apply` so the FX driver reads from one canonical source on both sides. New `SNAP_STATE_BOOSTING` bit at slot 14 of the existing u16 state_bits — fires leading-edge `SFX_JET_BOOST` on remote mechs in `snapshot_apply`, and the same audio fires owner-side from the boost-trigger seam in `mech_step_drive` (was defined in the manifest but never called — that gap is now closed). Two new SFX entries `SFX_JET_IGNITION_CONCRETE` / `SFX_JET_IGNITION_ICE` keyed on the `TILE_F_ICE` bit of the ground-impingement query at the just-ignited tick (rocket-takeoff thump). Ground-impingement dust is steam (pale cyan) on ice tiles and warm dust elsewhere. New `decal_paint_scorch` shares the existing blood splat queue + chunk-routing flush; scorch decals are permanent for the round (no per-decal fade — see TRADE_OFFS.md). Protocol id bumped `S0LJ` → `S0LK`. Audio assets `assets/sfx/jet_ignition_{concrete,ice}.wav` (49/49 SFX loaded post-P02; 47 base + 2 ignition) sourced via `tools/audio_inventory/source_map.sh` from `kenney_sci-fi-sounds` (`lowFrequency_explosion_000` + `thrusterFire_003`), normalized 22050 Hz mono PCM16 by `tools/audio_normalize/normalize.sh`. Halftone shader extended with shimmer pass: per-fragment UV displacement via two-octave sin/cos noise weighted by exp-falloff around each hot zone. Hot-reload watches `assets/sprites/jet_plume.png` + `assets/sprites/jet_dust.png` (missing-asset fallback: bright DrawLineEx along thrust_dir). See `documents/m6/02-jetpack-propulsion-fx.md`. M6 P01 replaces `build_pose` + `apply_pose_to_particles` with a deterministic procedural pose function (`src/mech_ik.c::pose_compute`) running after physics each tick — bones are a pure function of (pelvis, aim, anim_id, gait_phase, facing, chassis, dismember_mask, foregrip, grapple) so every client renders the same skeleton. New wire field `gait_phase_q` (u16); protocol id `S0LI` → `S0LJ`. Retires "Remote mechs render in rest pose on the client" + "Left hand has no pose target (no IK)" trade-offs. Also fixes the non-hitscan self-fire SFX bug (`src/net.c::client_handle_fire_event` `predict_drew` split into `_sparks` and `_sfx`). New tests `test-mech-ik` (16 asserts), `test-pose-compute` (27 asserts), `test-riot-cannon-sfx` (3 paired-shot asserts), `+5` `test-snapshot` round-trip asserts. See `documents/m6/01-ik-and-pose-sync.md`. P19 filled the P14 audio runtime with 62 CC0 assets (47 SFX + 1 servo + 7 music + 7 ambient) from Kenney + opengameart, sourced via `tools/audio_inventory/source_map.sh`, normalized to 22050 Hz mono PCM16 WAV / mono Vorbis OGG by `tools/audio_normalize/normalize.sh`, and gated by `make audio-inventory` / `audio-normalize` / `audio-credits` / `test-audio-smoke`. M6 (stability + ship prep) is next. M4 shipped the lobby & matches layer: full game flow (title → server browser → lobby → countdown → match → summary → next round); FFA + TDM + CTF modes; per-slot loadout picker (chassis / primary / secondary / armor / jetpack); ready/team toggles; LAN-broadcast server discovery; `soldut.cfg` for port + match defaults + map/mode rotations; three code-built maps (Foundry / Slipstream / Reactor) plus Crossfire (CTF arena). Underneath that the **M3** combat layer is intact: Verlet skeleton physics on a 16-particle skeleton; 5 chassis with passives; 8 primaries + 6 secondaries (Grappling Hook real as of P06); projectile pool; explosions; per-limb HP and dismemberment; recoil + bink + self-bink; armor + jetpack variants; friendly-fire toggle; kill feed UI. **M2** networking is in (authoritative server, client prediction + reconciliation, hitscan lag comp; LAN bake-test still pending). **M5 progress so far**: P01 — `.lvl` binary format + loader/saver (`src/level_io.{c,h}`); P02 — polygon collision + slope physics + slope-aware post-physics anchor; P03 — render-side accumulator + interp alpha + reconcile smoothing + two-snapshot remote-mech interp + `is_dummy` wire bit + hit/fire event sync; P04 — standalone level editor at `tools/editor/` (raygui, ear-clipping, slope + alcove presets, undo/redo, save validation, F5 test-play) + game-side `--test-play <path>` flag; P05 — pickup runtime (`src/pickup.{c,h}` + `PickupPool` on World), Engineer's `BTN_USE` deployable repair pack, Burst SMG queued 70 ms cadence, `PICKUP_PRACTICE_DUMMY` kind, three powerups (BERSERK / INVIS / GODMODE) with snapshot-bit mirroring, `NET_MSG_PICKUP_STATE` event protocol, Foundry default-map example pickups, and active-powerup HUD pill; P06 — grappling hook (per-Mech `Grapple` state, `PROJ_GRAPPLE_HEAD` projectile, new `CSTR_FIXED_ANCHOR` constraint kind, one-sided contracting distance constraint for the Tarzan-swing feel, BTN_JET retract at 800 px/s clamped to `[60, 300]` rest length, chain re-fire while ATTACHED, `SNAP_STATE_GRAPPLING` bit gating an 8-byte trailing wire suffix on `EntitySnapshot`, straight-line gold rope render); P07 — CTF mode (`src/ctf.{c,h}`, `Flag flags[2]` on World, both-flags-home capture rule, 36 px touch / 30 s auto-return, half-jet + secondary-disabled carrier penalties, drop-on-death from `mech_kill`, mode-mask validation in `start_round`, `NET_MSG_FLAG_STATE` 26-byte event protocol with INITIAL_STATE suffix, team-colored staff-and-pennant render with off-screen HUD compass); P08 — map sharing across the network (`src/map_cache.{c,h}` + `src/map_download.{c,h}`, 36-byte `MapDescriptor` appended to `INITIAL_STATE`, four new lobby-channel wire messages — `NET_MSG_MAP_REQUEST`/`MAP_CHUNK`/`MAP_READY`/`MAP_DESCRIPTOR`, 1180-byte reliable chunks, content-addressed cache at `<XDG_DATA_HOME>/soldut/maps/<crc>.lvl` with 64 MB LRU, lobby UI download progress strip, host countdown gate on per-peer `MAP_READY`); P08b — runtime `MapRegistry` (32-slot cap) replaces the static 4-entry `MapId` enum; `game_init` scans `assets/maps/*.lvl` (cheap header + META read, no `level_load`) so user-authored maps surface in the host's lobby cycle without overwriting a builtin slot, and the four named constants (`MAP_FOUNDRY`..`MAP_CROSSFIRE`) stay as load-bearing reserved indices for `build_fallback`; P09 — controls + UI: `BTN_FIRE_SECONDARY` (RMB) one-shot of inactive slot via `fire_other_slot_one_shot` (shared `fire_cooldown` gates LMB+RMB so DPS doesn't double; charge weapons rejected; carrier secondary still gated), host `[Kick] [Ban]` modal in the lobby player list with persistent `bans.txt` (`<addr_hex> <name>` per line), three-card map vote picker on the summary screen (Fisher-Yates over `g_map_registry`, `NET_MSG_LOBBY_VOTE_STATE`, winner overrides `match.map_id` on `begin_next_lobby`), and a title-screen Controls modal sourced from `src/platform.c`; resolves three M4-era trade-offs (vote picker UI, kick/ban UI, `bans.txt` persistence); P10 — mech sprite atlas runtime + per-chassis distinctness: aggressive bone-length distinctness pass in `g_chassis[]` (Heavy bumped torso 34→38 + neck 14→16, Scout shrunk -16% all over, Sniper anisotropic with forearm 17→19 + shin 18→21, Engineer compact with forearm 16→14 + thigh 18→16), per-chassis posture quirks in `build_pose` (Scout forward lean, Heavy chest strength 0.7→0.85, Sniper head down+forward 3 px, Engineer skips right-arm aim when active_slot=1), new `src/mech_sprites.{c,h}` with `MechSpriteSet g_chassis_sprites[CHASSIS_COUNT]` (22-entry sub-rect/pivot/draw-size table per chassis), sprite-or-capsule dispatch in `draw_mech` (new `draw_mech_sprites` walks 17-entry `g_render_parts[]` z-order with L↔R swap when `facing_left`; old M4 path preserved as `draw_mech_capsules` and fires when `atlas.id == 0`), held-weapon line factored into shared `draw_held_weapon_line` until P11, atlas loaded in `main.c` + `shotmode.c` after `platform_init`, new shotmode directive `extra_chassis` + shot test `tests/shots/m5_chassis_distinctness.shot` (capsule-rendering trade-off stays open until P12 ships damage feedback); P11 — per-weapon visible art runtime: new `src/weapon_sprites.{c,h}` with 14-entry `g_weapon_sprites[]` table (sub-rect + grip / foregrip / muzzle pivots in source-relative px + draw_w/h), shared `Texture2D g_weapons_atlas` from `assets/sprites/weapons.png` (line fallback when missing — `draw_w * 0.7` per-weapon-sized barrel keeps visible distinctness without the atlas), `weapon_muzzle_world(...)` + `weapon_foregrip_world(...)` helpers (sprite-local `+X` along aim, `+Y` perpendicular `(-aim.y, aim.x)`), new `draw_held_weapon` in `src/render.c` (called from `renderer_draw_frame` after `draw_mech`, before `draw_grapple_rope`) picks an effective weapon via new `Mech.last_fired_slot` / `last_fired_tick` (RMB-fire-secondary triggers a 3-tick hand-flicker swap to the inactive slot's sprite, then reverts) + visible muzzle flash (additive yellow-orange disc, alpha-tapered over 3 ticks), four fire paths (`weapons_fire_hitscan`, `_lag_comp`, `weapons_predict_local_fire`, `weapons_spawn_projectiles`) plus both grapple branches in `mech.c` migrated from `Weapon.muzzle_offset` (single float kept as fallback) to `weapon_muzzle_world(...)` so visible muzzle and physics muzzle coincide, new shotmode directive form `extra_chassis <name> <x> <y> ["primary weapon"]` (optional primary override for side-by-side weapon comparison shots), new shot tests `tests/shots/m5_weapon_held.shot` (pristine / LMB-flash / RMB-flicker / swap states + contact sheet) + `m5_drift_isolate.shot` (Trooper standing still 200 ticks; pelvis settles to v=0 by t=59 and stays put — regression check). Two-handed foregrip pose was attempted (three variants: strength-0.6 L_HAND yank, clamped, snap-pose-IK strength-1.0) and reverted post-ship — all three drifted the body in the aim direction during steady-hold because the L_ARM rest-state decouples from L_SHOULDER during the constraint-solve iterations as PELVIS shifts under R_ARM aim drive. Real fix is a 2-bone IK constraint INSIDE the solver loop; deferred to M6 per amended TRADE_OFFS.md "Left hand has no pose target". `WeaponSpriteDef.pivot_foregrip` stays on the table for the future IK consumer; L_HAND dangles for both one-handed and two-handed weapons at post-P11 ship; P12 — damage feedback layers: hit-flash white-additive blend (`Mech.hit_flash_timer = 0.10 s` set on every successful damage event in `mech_apply_damage`, decayed each tick in `simulate_step`, blended toward white in BOTH render paths via `apply_hit_flash` in `render.c`), persistent per-limb damage decals (`Mech.damage_decals[17]`, 16-deep oldest-overwrite ring per visible part, sprite-local i8 coords, three kinds DENT/SCORCH/GOUGE keyed off damage amount, composited by `draw_damage_decals` with `part_local_to_world` re-rotating against the current bone angle so decals migrate with the body), 64-particle radial blood spray on `mech_dismember` (replacing M3's 32-particle `blood_spew_at`) + new `FX_STUMP` pinned emitter (`pin_mech_id` + `pin_limb` on `FxParticle`, ~1.5 s lifetime, tracks the parent NECK/SHOULDER/HIP particle and spawns 1–2 downward-biased blood drops per tick), `fx_spawn_smoke` from limbs <30% HP with `intensity = ((0.30 - frac) / 0.30)^2` RNG roll on every 8th tick, stump-cap render via `MSP_STUMP_*` sub-rects in `draw_mech_sprites` (sprite-path only — capsule fallback intentionally has no stump art per spec), `mech_part_to_sprite_id` + `mech_sprite_part_endpoints` helpers in `mech_sprites.{c,h}` keep the world↔local transform consistent between record-time (`mech_apply_damage`) and render-time (`draw_damage_decals`), client-side `NET_MSG_HIT_EVENT` handler now also kicks `hit_flash_timer` + decrements limb HP + appends to the same per-limb decal ring + routes newly-set `dismember_mask` bits through `mech_dismember` so host and client run the same constraint deactivation + spray + emitter sequence (verified via `tests/shots/net/2p_dismember.{host,client}.shot`); the capsule-rendering trade-off entry's deletion gate moved from P12 to P15/P16 in `TRADE_OFFS.md` (no `assets/sprites/<chassis>.png` files exist on disk until then, so the capsule fallback is what fires in real play through P14 development). P13 — rendering kit: TTF fonts (`Atkinson-Hyperlegible-Regular.ttf` + `VG5000-Regular.otf` + `Steps-Mono-Thin.otf` vendored under `assets/fonts/`, loaded via `LoadFontEx` in `platform_init`, exposed through new `g_ui_font_body / _display / _mono` globals + `UIFontKind` enum + `ui_font_for(kind)` helper with graceful `GetFontDefault()` fallback when a face fails to load — `src/ui.c` and `src/hud.c` migrated; the `Default raylib font (no vendored TTF)` M4 trade-off is gone), halftone post-process (`assets/shaders/halftone_post.fs.glsl` GLSL 330 with 8x8 Bayer dither + 30%-density screen, wired via a new backbuffer-sized `g_post_target` `RenderTexture2D` + `g_halftone_post` `Shader` in `src/render.c`; world draws to RT, backbuffer blit applies shader, HUD draws on top with no shader; missing shader file → graceful direct-to-backbuffer fallback), per-map kit module `src/map_kit.{c,h}` (`g_map_kit` global with parallax_far/mid/near + tiles `Texture2D` slots loaded from `assets/maps/<short>/`, hooked from `map_build` / `map_build_from_path` after `level_load` succeeds, no-asset fallbacks for every slot), 3-layer parallax draw (far + mid screen-space before BeginMode2D at ratios 0.10/0.40, near after at 0.95, `fmodf`-based wraparound), tile sprites via `draw_level_tiles` (32x32 sub-rects from 8x8 grid keyed off `LvlTile.id`, 2-tone checkerboard fallback), free polygon rendering refactored out of the P02 stopgap into `draw_polys` (M5 spec colors per kind: SOLID 32,38,46 / ICE 180,220,240 / DEADLY 80,200,80 / ONE_WAY 80,80,100) + `draw_polys_background` (BACKGROUND polys after mechs at alpha 0.6 as foreground silhouettes), decoration sprites via lazy-loaded `g_decorations_atlas` (4-layer dispatch: 0+1 before tiles, 2 after tiles before mechs, 3 after mechs/projectiles/fx; ADDITIVE flag wraps in `BeginBlendMode(BLEND_ADDITIVE)`; FLIPPED_X flips source rect; sub-rect picked via `sprite_str_idx` hashed into 16x16 grid of 64x64 cells until P15/P16 ships a real manifest; missing-atlas fallback paints layer-tinted placeholder rectangles so designers see deco placement during test-play), HUD final art (lazy-loaded `g_hud_atlas` from `assets/ui/hud.png`, new `draw_bar_v2` with 1px outline + dark bg + fg fill + tick marks every 10% — works in both atlas + no-atlas paths since the layout is identical, `hud_draw_weapon_icon` + `hud_draw_killflag_icon` with per-id color-swatch / short-text fallbacks, kill-feed entries replaced text-only weapon name with weapon-icon + name + flag-icon variants, crosshair tints by bink — red >0.5 / pale-cyan otherwise — atlas-aware sprite when present + line-cross fallback, all HUD text migrated to `DrawTextEx` with `ui_font_for`), decal layer chunking in `src/decal.c` (levels ≤4096 px keep single-RT M4 path; levels >4096 px partition into 1024×1024 chunks with `DECAL_MAX_CHUNKS=64` cap and lazy `ensure_chunk_alloc(cx, cy)` on first paint so untouched zones cost zero memory — typical Citadel-sized match stains 3–8 chunks = 12–32 MB peak inside the 80 MB texture budget; `decal_paint_blood` queues into pending list, `decal_flush_pending` walks chunks + finds overlapping splats + batches paints inside one BeginTextureMode pair per dirty chunk), and dev-builds-only hot reload via new `src/hotreload.{c,h}` (mtime watcher polling every 250 ms, DEV_BUILD-gated — Makefile DBG_CFLAGS adds `-DDEV_BUILD`, release builds compile every public function as a no-op; main.c registers callbacks at startup for chassis × 5 + weapons + decorations + HUD + halftone shader, fires `unload + load` on file change after first sighting). Asset gen (P15–P16) shipped. **wan-fixes 1–15** (off-roadmap, 2026-05-11): wan-fixes-1 — server-side hitscan lag-comp + 60 Hz snapshots (was 30) + redundant input packets bundled into every `NET_MSG_INPUT` + grapple-head lag-comp; wan-fixes-2 — bundle `assets/` in artifact zip + restore 100 ms render interp; wan-fixes-3 — remote mechs kinematic on the client (`inv_mass=0`, only `snapshot_interp_remotes` moves them); wan-fixes-4 — `SNAP_STATE_RUNNING` wire bit + gait-lift hysteresis stabilizes remote walk anim; wan-fixes-5/6/7 — "Host Server" UI spawns a `--dedicated PORT` child of the same binary, host UI joins as a regular client, host setup (mode / map / score / time / ff) forwarded as CLI args, UI cache re-published on re-host; wan-fixes-8 — `soldut-prefs.cfg` key=value persistence for name + loadout + team + connect_addr (atomic write via `<path>.tmp` + rename, cwd-relative like `soldut.cfg`/`bans.txt`); wan-fixes-9 — async "Starting server..." overlay during dedi spawn (split `bootstrap_host_via_dedicated` into begin / poll / abort driven from the main loop); wan-fixes-10 — new `NET_MSG_EXPLOSION = 17` wire event (7 bytes, pos at 1/4 px quant + owner mech + weapon_id) drives client visuals at the SERVER's authoritative pos instead of the client's rest-pose-against-rest-pose-bones local detonation (which sat ~9 px off); same PR also removed the `sync_loadout_from_server` snap branch that was silently reverting prefs to server defaults on first connect; wan-fixes-11 — lobby polish: cycle buttons take LMB forward / RMB back with `<` / `>` edge arrows (new `ui_cycle_button` + `UIContext.mouse_right_pressed`); Ready Up gets pulsing-green chrome distinct from the loadout rows; "Loading match..." full-screen overlay covers the COUNTDOWN-end → first-snapshot gap; wan-fixes-12 — text-input cursor navigation: `UIContext.caret_pos` persists across frames, Left/Right/Home/End/Delete + mid-string insertion in `ui_text_input`; wan-fixes-13 — kill-event wire format widened 7 → 39 bytes (adds `u8 flags` + two 16-byte name strings — server fills names from lobby slots in `apply_new_kills`, client decodes and appends to its local `world.killfeed[]` exactly like `mech_kill` does on the server); HUD reads names from the entry, lifetime 6 → 15 s with 3 s linear-fade tail, layout anchored to the top-right corner (right of the centered match banner — DPI-aware: `x = sw/2 + 220*sc + 16*sc`); default window size 1280×720 → **1920×1080** so the HUD lands at a legible scale out of the box; wan-fixes-14 — loadout + team picker disable once Ready Up commits (passes `enabled = !me_ready` to `ui_cycle_button`; the existing disabled branch returns 0 and paints dim); Ready button stays clickable as the escape hatch; mode/map/score/time/ff stay editable for the host because those are lobby-wide; wan-fixes-15 — HP numerals font 18 → 22 px (Steps Mono), armor bar lifted to `hp_text_y - 10 - armor_h` so its bottom edge no longer clips the top of the HP digits (was: 4-px overlap); wan-fixes-16 — "Host Server" UI uses an IN-PROCESS server thread instead of a spawned child of Soldut.exe. Windows silently drops UDP between any pair of processes that share a spawn-tree ancestor (proved with `bInheritHandles=FALSE` + `CREATE_NO_WINDOW` + `CREATE_BREAKAWAY_FROM_JOB` + launcher-pattern PPID severance + `PROC_THREAD_ATTRIBUTE_PARENT_PROCESS` spoofing to Explorer — none fixed the bug, all confirmed across two machines). Same-process UDP loopback DOES work (proven with a dedi self-loopback probe). So the server runs on a separate thread of the host UI process: `dedicated_run(opts, args)` accepts a `DedicatedRunOptions { standalone, quit_flag, log_prefix }` so the same body serves the standalone `--dedicated` CLI and the in-process thread; `in_process_server_start` spawns the thread, polls a `s_in_proc_server_ready` flag until `net_server_start` binds, then returns so the client (the UI main thread) connects to `127.0.0.1:<port>` like any other peer. Two separate `Game` structs, zero shared memory between threads — communication strictly via the kernel UDP socket pair, host has no zero-latency advantage. Windows `CreateThread`/`WaitForSingleObject` lives in `proc_spawn.c` so main.c keeps avoiding `<windows.h>` (raylib type collisions). The `proc_spawn_via_launcher` / `--launch-dedicated` / parent-process-spoofing paths from the diagnostic rounds are removed; `proc_spawn`/`proc_spawn_self` stay as basic process utilities for test harnesses. `decal_init` is gated standalone-only because `LoadRenderTexture` needs the main thread's GL context. New regressions shipped: `tests/shots/net/run_frag_grenade.sh` (paired-dedi grenade sync, 9 assertions, `-b` for both throw directions), `tests/shots/net/run_kill_feed.sh` (kill-event end-to-end, 8 assertions). New visual previews: `make host-overlay-preview`, `make lobby-overlay-preview`. New on-disk per-user state: `/soldut-prefs.cfg` (gitignored). Protocol id stays `S0LG` (all additions are length-prefixed or gated on existing state bits). **P17** — author maps 1-4 as `.lvl` via `tools/cook_maps/cook_maps.c` (replaces the P01 stub; `make cook-maps` builds + runs it). Output: `assets/maps/{foundry,slipstream,reactor,concourse}.lvl`. Each emitted file round-trips through `level_load` + `level_build_poly_broadphase` inside the cooker (cook_one verify arena). Foundry (100×40, 16645 B; 45° spawn ramps, 30° hill, 2 edge alcoves, 8 spawns, 12 pickups), Slipstream (100×50, 20758 B; 60° basement chutes, ICE catwalk patches, 3 angled struts, 4-room cave network, 2 WIND zones, 14 pickups), Reactor (110×42, 19233 B; 30° bowl floor, 45° pillar overhangs, flanking ramps, 16 pickups), Concourse (100×60, 24965 B; programmatic scaffold per the new TRADE_OFFS entry "Concourse is synthesized programmatically, not editor-authored"; two 30° hills, 4 cover columns, 2 FOG zones, 4 alcoves, 18 pickups). `.gitignore` narrowed so the four authored `.lvl` files track while scratch/editor fixtures stay ignored. Two cross-build sync fixes landed under the same prompt: **sync-fix** — `snapshot_apply` now re-syncs `armor_id` + `jetpack_id` + `fuel_max` + `armor_hp_max` every applied snapshot (not just at spawn via `ensure_mech_slot`); `fuel` + `armor_hp` preserved as fractions of the new caps. Alignment of `ensure_mech_slot` clamp defaults with `server_handle_lobby_loadout` (`JET_STANDARD` / `ARMOR_LIGHT` instead of `JET_NONE` / `ARMOR_NONE`) removes silent client↔server divergence on out-of-range bytes. **map-sync** — `map_alloc_tiles` (used by every code-built fallback) + `level_build_tutorial` now `memset(Level, 0, sizeof *Level)` at the top, matching what `level_load` already does at `src/level_io.c:504` after parsing the .lvl header. Pre-fix the code-built fallback path inherited `poly_count` / `pickup_count` / `spawn_count` / `flag_count` and the corresponding pointers from whatever map was last loaded; with `arena_reset` between builds the pointers aliased freshly-reset arena memory — producing ghost polygons (rendering 6 garbage triangles of "polygons" that were actually a previous map's polygon storage repurposed as tiles) and ghost pickups (`add_pickup_to_map`'s `if (!L->pickups)` allocator check fell through on the stale pointer, appending onto a stale `pickup_count` instead of starting fresh — series of builds produced 5 → 10 → 15 → 16-cap pickups for the same Crossfire build). Two clients on the same `map_id` rendered different geometry whenever their lobby-cycle histories differed. New `DIAG-sync:`-prefixed `LOG_I` lines tag every loadout-sync + map-build seam (`lobby_add_slot` / `server_handle_lobby_loadout` / `lobby_spawn_round_mechs` / `ensure_mech_slot` / `snapshot_apply re-sync` / `map_build` result / `map_build_for_descriptor` decision) for paired-log debugging; new `local_mech_id stuck` 2-second `WARN` detector fires if the late-bind fails to resolve. `tests/net/run_3p.sh` switched from Foundry to Reactor per P17 spec (10/10 PASS).

**P18** (2026-05-12) — Catwalk / Aurora / Crossfire / Citadel cooked programmatically via `tools/cook_maps/cook_maps.c` (`build_catwalk` 60° slide slopes + jetpack alcoves + WIND zones, 22 pickups; `build_aurora` 30° hills + 45° pit + sky struts + ZERO_G + skyline silhouettes, 25 pickups; `build_crossfire` mirror-symmetric CTF + `LvlFlag` pair + flank tunnels + central-high POWERUP_INVISIBILITY alcoves, 32 pickups; `build_citadel` two carved-out castle dungeons + 30° plaza bowl + 60° ramparts + 45° castle ceilings + 12 grapple anchors + 4 WIND + 2 ACID + POWERUP_GODMODE in tunnel chokes, 31 pickups). New helpers `push_flag` + `push_deco` + `render_thumb` (256×144 PNG per map for the vote picker). New `tools/bake/run_bake.c` headless harness (crude wander+shoot bot AI, `make bake`/`make bake-all`, heatmap PNG + CSV + summary outputs; verdict informational not gating per the new TRADE_OFFS entry "Bake-test verdict is informational, not gating (P18)"). Existing "Concourse is synthesized programmatically" trade-off renamed and expanded to cover all 8 cook_maps maps (P17 + P18). Audio assets (P19) remain. See `documents/11-roadmap.md` and `documents/m5/`.

## Build & run

Native build (Linux/macOS host):

```bash
make                 # release build → ./soldut
./build.sh dev       # debug build with -O0 + ASan/UBSan
./build.sh release   # explicit release path through build.sh
make clean           # remove build artifacts (keeps third_party libs)
make distclean       # also rebuild raylib + enet from source
```

Cross-compile (requires `zig` on PATH):

```bash
make windows         # → build/windows/Soldut.exe (also runs cross-windows.sh)
make macos           # → build/macos/Soldut.app  (needs macOS SDK)
```

Note: `cross-windows.sh` wipes and re-builds `third_party/raylib` and `third_party/enet` for the Windows target, then *restores* the host-native raylib build at the end. If you interrupt it, run `make distclean && make raylib && make enet` to recover the host libs.

## Tests

Multiple regression harnesses (all via `make`):

```bash
make test-physics            # tests/headless_sim.c — humans read; does NOT assert
make test-level-io           # .lvl round-trip + CRC32 (asserts)
make test-pickups            # pickup state machine (asserts)
make test-ctf                # CTF rules — pickup/return/capture (52 asserts)
make test-snapshot           # snapshot encode/decode round-trip
make test-spawn              # spawn placement
make test-prefs              # soldut-prefs.cfg round-trip
make test-map-chunks         # chunk reassembly + bit-flip CRC fail (21 asserts)
make test-map-registry       # MapRegistry scan + lobby-rotation surfacing
make test-map-share          # end-to-end host → client .lvl stream
make test-editor             # editor binary smoke
make test-spawn-e2e          # editor → host → client spawn flow
make test-meet-named         # editor → host → client custom-map join
make test-meet-custom        # same, untitled-map path
make test-ctf-editor-flow    # editor-authored CTF map round-trip
make test-grapple-ceiling    # grapple against angled-ceiling polys
make test-frag-grenade       # paired-dedi grenade sync (tests/shots/net/run_frag_grenade.sh)
```

`tests/headless_sim.c` is the **outlier**: it dumps particle positions for human inspection and does *not* exit non-zero on regression. Every other target above asserts and returns a non-zero exit on failure. Paired-process flows (`tests/net/run.sh`, `tests/shots/net/run.sh`) spawn a real host + client over loopback and assert on log lines or paired screenshots.

CI builds on Linux/Windows-cross/macOS but does not run any of these (see TRADE_OFFS.md → "No CI for physics correctness").

## Visual debugging — shot mode

For motion/physics questions where text dumps aren't enough, the binary
takes a `--shot path/to/script.txt` flag that drives a scripted scene
through the real renderer and writes PNGs to `build/shots/`.

```bash
./soldut --shot tests/shots/walk_right.shot
make shot                                    # default script above
make shot SCRIPT=tests/shots/your_case.shot  # any other .shot
```

A script is line-based; comments start with `#`. Header directives:
`window <w> <h>`, `seed <hi> <lo>`, `out <dir>`, `spawn_at <wx> <wy>`
(teleport the player after world build, before tick 0 — useful for
focusing tests on a specific location), plus one of
`aim <wx> <wy>` (world-space, fixed target) or `mouse <sx> <sy>`
(screen-space cursor; converted via the live camera each tick — same
path as real play, so camera-follow changes the world target as the
mech moves). Tick-stamped events: `at <tick> press|release|tap
<button>`, `at <tick> aim <wx> <wy>`, `at <tick> mouse <sx> <sy>`,
`at <tick> shot <name>` (writes `<out>/<name>.png` after that
tick's render), `at <tick> end`. Range directives: `burst <prefix>
from <t0> to <t1> every <k>` (one shot per k ticks) and
`mouse_lerp <sx> <sy> from <t0> to <t1>` (linear cursor sweep). The
`contact_sheet <name> [cols <C>] [cell <W> <H>]` directive composites
every shot from the run into a single grid PNG at `<out>/<name>.png`
— useful for sending many frames to an LLM at the token cost of one
image (default 320×180 cells × 4 cols).
Buttons: `left right jump jet crouch prone fire reload melee use
swap dash`. Use `mouse` for accurate gameplay reproduction; `aim`
when you specifically need a world target that doesn't move with the
camera.

The runner uses a fixed `dt = 1/60`, doesn't read wall-clock time, and
re-seeds the World RNG when a `seed` directive is present — so a re-run
produces the same PNGs. See `src/shotmode.c`.

Each run also writes a `<scriptname>.log` next to its PNGs, with a
per-tick summary line for the local mech plus event lines for anim
transitions, grounded toggles, jet ceiling taper, post-physics anchor
recoveries, pose-sweep clamps, inside-tile escapes, hitscan
fire/hit/miss, damage, dismemberment, and kills. Use the `SHOT_LOG()`
macro from `src/log.h` to add more events; it's gated on
`g_shot_mode` (a one-branch no-op outside shot mode), so it's free to
sprinkle into hot paths.

## Required reading for non-trivial work

Three files near the root are not optional:

- **`documents/`** — the design canon. 12 numbered docs, read in order. Source of truth for *intent*. `documents/01-philosophy.md` and `documents/09-codebase-architecture.md` are the load-bearing ones for code style.
- **`CURRENT_STATE.md`** — what the build actually does *right now*, including tunable values and the most recent debugging round. Update this when behavior changes.
- **`TRADE_OFFS.md`** — deliberate compromises between intent and reality, with revisit triggers. Add an entry *before* shipping a "for now" hack; *delete* the entry when the proper fix lands (this is a queue, not a changelog).

If the docs disagree with the code about *what* it does, the code wins and the doc is wrong — fix the doc. If they disagree about *how* code should be written, the philosophy doc wins.

## Architecture in a paragraph

`main.c` runs a fixed-step 60 Hz accumulator; each tick calls `simulate(World*, ClientInput, dt)` (in `src/simulate.c`), which is the pure simulation step — no globals, no wall-clock reads, just the World's seeded PCG RNG. The simulate step does pose drive (`mech_step_drive`) → try-fire (`mech_try_fire` dispatches by `WFIRE_*` kind) → latch prev_buttons → gravity → Verlet integrate → 12-iter constraint+collision relaxation (interleaved in one loop, see `physics_constrain_and_collide`) → `mech_post_physics_anchor` (the standing-pose hack — see TRADE_OFFS.md) → `projectile_step` (integrate, sweep-collide vs tiles + bones, detonate AOE) → FX update → kill-feed aging → bookkeeping. Render reads the latest `World` and draws (including projectiles); render never writes authoritative state. Module dependency graph is a DAG with `world` at the top and `arena`/`pool`/`log`/`math`/`hash`/`ds` as leaves; `render`/`hud`/`lobby` are consumers only. `weapons.c` and `projectile.c` are peers (weapons spawn projectiles; projectiles call back into `mech_apply_damage` and `explosion_spawn`).

## Conventions that bite if you ignore them

- **C11 only.** `-std=c11 -Wall -Wextra -Wpedantic -Werror`. No C++, no scripting, no codegen, no threads at v1, no `void *` in public APIs for typed data.
- **One `.h` + one `.c` per module.** Public surface in the header; everything else is implementation. No `_internal.h`, `_types.h`, `_priv.h` splits unless a `.c` exceeds ~1500 LOC.
- **Allocate up front.** `malloc` is called only at `arena_init` / pool init at startup and at level load. Three arenas: `permanent` (process), `level_arena` (round), `frame_arena` (one tick, reset every tick). Pools have stable indices used as identifiers.
- **SoA where it matters.** `ParticlePool` is parallel arrays of `pos_x`, `pos_y`, `prev_x`, `prev_y`, `inv_mass`, `flags`. Don't refactor it into AoS to "look cleaner."
- **No malloc in inner loops.** Grow the right arena instead.
- **Naming tells cost.** `physics_solve_world` is expensive; `mech_predict_local` is cheap; `level_load_from_disk` is slow + IO. Refactor the name first if it lies.
- **Pose drive is kinematic translate, not position-only.** When you move a particle to fit a pose, update **both** `pos` and `prev` by the same delta (use `physics_translate_kinematic`) — otherwise Verlet reads the displacement as injected velocity. This bug class burned a full debug round in M1; see CURRENT_STATE.md → "Recently fixed."
- **`Game *g` is the only spine.** Everything threads through it; no module-level mutable globals (raylib's globals are the one accepted exception).

## Tunables live in code

The numbers driving feel are `#define`s in `src/physics.h`, `src/mech.c`, and `src/weapons.c`. `CURRENT_STATE.md` mirrors them in a table for quick reference but the C files are authoritative. The simulation runs at **60 Hz**, not 120 Hz as `documents/03-physics-and-mechs.md` specifies — this is a tracked trade-off.

## Vendored dependencies (do not add a fifth without writing it down)

- `third_party/raylib/` — built into `libraylib.a` per platform via its own Makefile
- `third_party/enet/` — `*.c` compiled into `libenet.a` by our top-level Makefile
- `third_party/stb_ds.h` and `third_party/stb_sprintf.h` — header-only; `src/ds.c` is the one file that `#define STB_DS_IMPLEMENTATION`s
- `third_party/raygui/raygui.h` — header-only immediate-mode UI on top of raylib. Used **only by the level editor** (`tools/editor/`); the game proper does not link it. Vendored at M5 P04. License: zlib/libpng (same as raylib).

The game ships as a single static binary per platform. No DLLs, no plugins, no scripting runtime. The level editor ships as a separate binary (`build/soldut_editor`) — same engine, different `main()`.
